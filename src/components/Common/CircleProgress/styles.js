import styled from 'styled-components'
import { CircularInput } from 'react-circular-input'

export const CircularInputWrapper = styled(CircularInput)`
  width: 113px;
  height: 113px;
  position: absolute;
`
